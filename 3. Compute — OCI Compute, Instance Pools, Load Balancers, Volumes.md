# 3. Compute — OCI Compute, Instance Pools, Load Balancers, Volumes

#7_mystudy  
**OCI Architect Professional — Compute and availability**

### Translation from AWS Compute notes

| AWS memory anchor | OCI service/pattern | OCI distinction |
| --- | --- | --- |
| EC2 | OCI Compute instances | Select VM, bare metal, or flexible shapes based on CPU, memory, networking, licensing, and operational requirements. |
| Auto Scaling Group | Instance pool + autoscaling | Instance pools manage like instances; autoscaling adjusts pool size. Spread across fault domains and availability domains deliberately. |
| ELB/ALB/NLB | Load Balancer / Network Load Balancer | Load Balancer supports L7/L4 features; Network Load Balancer distributes based on L3/L4 data and is not simply an AWS NLB clone. |
| EBS | Block Volume | Design performance, attachment, backups, clones, replication, encryption, and availability requirements. |
| Instance store | Local NVMe / ephemeral local storage where supported | Local data is ephemeral; persist durable data in Block, File, or Object Storage. |
| Reserved/Savings/Spot | On-demand, committed-use/contract options, preemptible capacity where available | Evaluate current OCI commercial options; do not transfer AWS purchase-type mechanics into an OCI exam answer. |

## Compute selection and lifecycle

- Use VM shapes for general workloads; use bare metal, GPU, HPC, or specialized shapes only when workload requirements justify them.
- Flexible shapes let you select OCPUs and memory within shape limits. Capacity and shape availability are regional/AD considerations.
- Use custom images, instance configurations, and instance pools for consistent fleet deployment.
- Plan maintenance, boot-volume backup, OS patching, IAM instance principals, and observability from the start.
- Do not keep durable application state only on an instance's local/ephemeral storage.

## High availability and scaling

- Availability domains are physically isolated data centers in regions that have multiple ADs; fault domains are isolation groups within an AD.
- Spread an instance pool across fault domains, and across availability domains when the region/service architecture supports the required level of resilience.
- Place a Load Balancer in front of healthy backends. Health checks must reflect readiness; unhealthy backends are removed from rotation.
- Instance-pool autoscaling responds to a defined metric/schedule. Capacity scaling does not solve database, session-state, or downstream bottlenecks.
- Use regional DR architecture when an AD-level design cannot meet the required RTO/RPO.

## Storage with Compute

- **Boot Volume:** persistent boot disk; back it up separately from the instance.
- **Block Volume:** durable block storage for single/multi-attach patterns supported by the service; use backups/clones and volume groups for recovery workflows.
- **File Storage:** managed shared NFS file system.
- **Object Storage:** object/data-lake/backup distribution pattern, not a mounted block volume replacement.

## Recall exercises

- [ ] Design an application fleet that survives a fault-domain failure. Identify pool placement, Load Balancer health checks, state externalization, and volume recovery.
- [ ] Choose Block Volume, File Storage, Object Storage, and local storage for database data, shared web assets, backups, and scratch space.
- [ ] Explain why an Instance Pool/autoscaling solution does not by itself deliver multi-region DR.

## Official OCI references

- [OCI Compute](https://docs.oracle.com/en-us/iaas/Content/Compute/home.htm)
- [Instance pools](https://docs.oracle.com/en-us/iaas/Content/Compute/Tasks/creatinginstancepool.htm)
- [Load Balancer](https://docs.oracle.com/en-us/iaas/Content/Balance/Tasks/managingloadbalancer.htm)
- [Block Volume](https://docs.oracle.com/en-us/iaas/Content/Block/home.htm)

#OCI

